# Executive Summary

This is a sample reviewed draft section served as `text/markdown`.

It exists to give MSW download handlers a deterministic body that
exercises the `apiFetch({ parseAs: 'blob' })` path without dragging in
any real LLM output.
