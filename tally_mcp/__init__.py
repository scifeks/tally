# This local mcp/ package shadows the installed `mcp` SDK whenever the project
# root is on sys.path. See server.py for the import ordering workaround.
# todo: just rename to tally-mcp and invoke as a module
